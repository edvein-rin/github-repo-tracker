import { create } from "./create";
import { getAll } from "./getAll";
import { reload } from "./reload";
import { remove } from "./remove";

export const repositories = { getAll, create, reload, remove };
